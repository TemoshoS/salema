import React from "react";
import { View, StyleSheet, Text } from "react-native";

const HomeScreen = () => {
  return (
    <View>
      <Text>Salema Home</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  bottom: {
    top: 100,
    backgroundColor: "#f3f3 ",
    
  },
});

export default HomeScreen;
